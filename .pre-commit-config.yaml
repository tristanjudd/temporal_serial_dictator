repos:
  - repo: local
    hooks:
      - id: format
        name: auto-fix + format (ruff)
        entry: task format
        language: system
        types: [python]
        pass_filenames: false

      - id: lint
        name: lint (ruff)
        entry: task lint
        language: system
        types: [python]
        pass_filenames: false

      - id: typecheck
        name: typecheck (mypy)
        entry: task typecheck
        language: system
        types: [python]
        pass_filenames: false

      - id: test
        name: test (pytest)
        entry: task test
        language: system
        types: [python]
        pass_filenames: false
